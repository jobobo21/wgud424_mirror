<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="wgud424_maui.MainPage"
             Title="Status">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label x:Name="NameLabel" FontSize="20"/>
            <Label x:Name="ProgressLabel" FontSize="Medium" TextDecorations="Underline"/>
            <ProgressBar x:Name="CourseProgress" ProgressColor="Navy"/>
            <Label x:Name="CUBreakDown" FontSize="Medium"/>
            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
