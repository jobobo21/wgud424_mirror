<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="wgud424_maui.Views.TermView"
             x:Name="TermPage"
             Title="TermPage">
    <VerticalStackLayout Margin="20, 10, 20, 20">

        <!-- Course Statistics Grid -->
        <Grid x:Name="CourseStatsGrid" Margin="0,20,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Frame x:Name="TotalFrame" Grid.Column="0" BackgroundColor="LightGray" Padding="10" Margin="5">
                <StackLayout>
                    <Label x:Name="TotalCoursesLabel" Text="0" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
                    <Label Text="Total Courses" FontSize="12" HorizontalOptions="Center"/>
                </StackLayout>
            </Frame>

            <Frame x:Name="ActiveFrame" Grid.Column="1" BackgroundColor="LightBlue" Padding="10" Margin="5">
                <StackLayout>
                    <Label x:Name="ActiveCoursesLabel" Text="0" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
                    <Label Text="Active" FontSize="12" HorizontalOptions="Center"/>
                </StackLayout>
            </Frame>

            <Frame x:Name="CompletedFrame" Grid.Column="2" BackgroundColor="LightGreen" Padding="10" Margin="5">
                <StackLayout>
                    <Label x:Name="CompletedCoursesLabel" Text="0" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
                    <Label Text="Completed" FontSize="12" HorizontalOptions="Center"/>
                </StackLayout>
            </Frame>
        </Grid>

        <ListView x:Name="CourseListView" 
                  HeightRequest="500" 
                  ItemSelected="CourseListView_ItemSelected" 
                  RowHeight="100"
                  SeparatorVisibility="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame Margin="10,5" 
                               Padding="15" 
                               BackgroundColor="White" 
                               HasShadow="True"
                               BorderColor="LightGray">
                            <StackLayout>
                                <Label Text="{Binding Course.code}" 
                                       FontSize="16" 
                                       FontAttributes="Bold"
                                       TextColor="DarkBlue"/>
                                <Label Text="{Binding Course.description}" 
                                       FontSize="12" 
                                       TextColor="Gray"
                                       Margin="0,5,0,0"/>
                            </StackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>