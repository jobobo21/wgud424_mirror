<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="wgud424_maui.Views.TermView"
             x:Name="TermPage"
             Title="TermPage">
    <VerticalStackLayout Margin="20, 10, 20, 20">
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Grid x:Name="CourseGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <ListView x:Name="CourseListView" HeightRequest="500" ItemSelected="CourseListView_ItemSelected" RowHeight="75">
            <ListView.ItemTemplate>
                <DataTemplate >
                    <ViewCell Height="100">
                        <StackLayout>
                            <Label Text="{Binding Course.code}"/>
                            <Label Text="{Binding Course.description}"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
    </VerticalStackLayout>
</ContentPage>